.sa-widget {
  &.jarviswidget-fullscreen-mode {
    width: 100%;
    height: 100%;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1050;

    .sa-widget-content {
      height: calc(100vh - 34px);
      overflow: auto;
    }
  }

  .jarviswidget {
    margin-bottom: 0;
  }
}
